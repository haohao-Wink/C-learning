#include <iostream>
#include <string.h>
using namespace std;

#if 0
class Person()
{

    // public\private\protect中添加函数和变量都可以

    // 公有
public:
    // 函数、变量（成员函数、数据成员）
    // 方式、行为（函数）
    void pay(float money);
    float get();

    // 自己使用
protected:
    // 私有，不对外提供
private:
    // 属性、表明的特征（变量）
    char name[20];
    int age;
    float money;
};

int main()
{
}

class 类名
{

public:
    \\数据成员
    \\成员函数

        protected :
    \\数据成员
    \\成员函数

        private :
    \\数据成员
    \\成员函数
};

#endif